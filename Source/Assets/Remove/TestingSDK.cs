using UnityEngine;
using System.Collections;
//using PlayFab;
//using PlayFab.ClientModels;

public class TestingSDK : MonoBehaviour {
    

	// Use this for initialization
	void Start () {
        //PlayFabSettings.TitleId = "2abe";
       // PlayFabClientAPI.LoginWithCustomID(new LoginWithCustomIDRequest(){ CustomId = "1234567890", CreateAccount = true}, (result) => { Debug.Log(result.PlayFabId);}, (error) => { Debug.LogError(error.GenerateErrorReport()); });


	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
